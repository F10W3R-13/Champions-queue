# CQ 참여율 진단 — 데이터 분석 + 구조적 한계 + 체크 계획

> 작성일: 2026-07-03 · 데이터 소스: Airtable (Records_HP 209건, Players 133건), CLAUDE.md/IMPROVEMENT_PLAN.md 설계 문서
> ⚠️ 이 분석은 **OCR 스크린샷 기준**이다. "경기가 안 열림"과 "스크린샷을 안 올림"을 구분하지 못한다. 진짜 매치 기록은 NeatQueue history — §4 체크리스트 1번이 최우선인 이유.

---

## 1. 데이터로 본 현재 상태 (2026-07-03 기준)

### 퍼널
| 단계 | 인원 | 전환율 |
|---|---|---|
| Players 등록 | 133명 | — |
| 경기 기록 1회 이상 (linked) | 36명 | **27%** |
| 최근 세션(6/21) 참여 | 27명 | 20% |
| 6/21 이후 기록 | **0명** | 0% |

**등록자의 73%가 첫 경기 기록 없이 사라진다.** 가장 큰 누수는 큐 안이 아니라 **등록 → 첫 경기 사이**에 있다.

### 세션 추이
| 날짜 | 매치 | 고유 플레이어 |
|---|---|---|
| 2026-06-12 | 8 | 15 |
| 2026-06-21 | 13 | 27 |
| 6/22 ~ 7/3 | **0** | **0** |

- 기록된 세션은 단 2회. 성장(15→27)했다가 **12일째 완전 침묵**.
- 6/12 참가자 15명 중 6명만 6/21 복귀 (**복귀율 40%**).
- 상위 10명이 전체 기록의 **50.7%** — 헤비유저 중심 구조.
- 신규 등록: 6/8~6/21에 124명, 6/22 이후 9명으로 급감.

### 데이터 이상 신호
- **Records_SND = 0건.** SND 경기가 한 번도 ingest되지 않음. 커뮤니티가 HP만 하는 게 아니라면 파이프라인/업로드 문제.
- unlinked 15건 + Unmatched 13건 + Needs Review 2건 (전체의 ~14%) — 스탯이 본인에게 안 붙는 경험.

---

## 2. 구조적 한계 (설계상 이탈을 유발할 수 있는 지점)

각 항목: **가설 → 왜 구조적인가 → 확인 방법**

### S1. 큐 성사에 10명 동시 접속 필요 (critical mass 함정)
5v5 fill-and-pop + 고정 세션 창(NA 23:00 ET / EU 23:00 CET) 구조라, 9명이 모이면 **9명 전원이 헛걸음**한다. 헛걸음 경험 → 다음 세션 불참 → 더 안 모임의 악순환. 참여율 하락의 가장 흔한 구조적 원인.
- **확인**: NeatQueue history에서 세션별 성사 매치 수 + (가능하면) 큐 진입 후 pop 실패 인원. RSVP 인원 vs 실제 매치 인원 비교 (`queue_state.json`의 RSVP 명단 — 서버).

### S2. 등록 → 첫 경기 퍼널이 계측 없이 다단계
Verify → `/ign` → Registered 역할 → 세션 시간 맞추기 → NeatQueue join → 매치 성사. 73% 누수가 이 구간인데, **어느 단계에서 새는지 볼 수 있는 데이터가 없다.** 등록만 하고 세션 시간(새벽/심야)이 안 맞아 못 뛰는 사람이 다수일 가능성.
- **확인**: Registered 역할 보유자 중 NeatQueue join을 한 번이라도 누른 사람 수 (NeatQueue 데이터). RSVP 명단 vs 등록자 명단 diff.

### S3. 결과 기록이 수동 스크린샷 업로드에 의존
스크린샷 2장 업로드는 매치마다 누군가의 수고. 안 올리면 스탯·Impact MMR·리더보드가 전부 빈다 → "올려봤자"가 되면 기록 시스템 전체가 무의미해지는 단일 실패점. SND 0건이 이미 그 증거일 수 있음.
- **확인**: NeatQueue 매치 수 vs Airtable Match ID 수 비교 (기록 커버리지 %). SND는 #results 채널 히스토리에서 SND 스크린샷 업로드 여부 육안 확인 → 업로드는 있는데 0건이면 OCR/ingest 버그.

### S4. Decay + 800 게이트가 저활동기에 역효과
Champs는 7일 grace 후 −10~−20/일. 세션이 주 1~2회뿐인 구조에서 **한두 번 빠지면 바로 감점 시작** — 쉬었다 돌아오려는 사람에게 "MMR 깎였네 + Registered 박탈"이 복귀 장벽이 된다. dead-day 면제가 있지만, 세션이 아예 안 열리는 지금 같은 기간에 의도대로 작동 중인지 확인 필요 (배포 6/26, 마지막 기록 매치 6/21 — 로컬 `decay_state.json`은 dead_days=0).
- **확인**: 서버의 `decay_state.json`에서 `dead_days`·`below_threshold` 실제 값. `/decaystatus`로 이탈자 몇 명의 MMR 확인 — 6/12 이탈자 9명이 감점으로 800 밑이면 구조적으로 복귀 불가 상태.

### S5. 헤비유저 집중 + 실력 양극화
상위 10명이 활동의 절반. 신규/캐주얼이 고인물과 붙어서 지는 경험 반복 → 복귀율 40%의 유력한 원인 후보.
- **확인**: 이탈자 9명 vs 잔류자 27명의 평균 Impact/K/D 비교 (성적 하위권이 주로 이탈했는지). NeatQueue mmr_change로 이탈 직전 연패 여부.

### S6. 관측성 공백 — 하락을 "느낌"으로만 아는 구조
현재 수집되는 건 **성사된 매치의 스크린샷뿐**. 큐 진입 시도, RSVP 대비 실참여, pop 실패, 세션 무산 같은 중간 지표가 없어서 참여율 문제를 진단할 데이터 자체가 없다. 이번 분석이 "기록 0"에서 막힌 것 자체가 이 한계의 증명.
- **해결**: §5 계측 추가 참조.

### S7. 시간대 구조 (EN/ES · NA/EU 서버인데 세션 창 각 1개)
Region 데이터가 거의 비어 있어(133명 중 표기 ~18명) 검증 불가지만, LATAM 유저에게 NA 23:00 ET 창이 맞는지, EU 유저 규모 대비 EU 세션이 실제 성사되는지 미지수.
- **확인**: Players.Region 채우기 + NeatQueue history에서 NA/EU 채널별 매치 수 분리 집계.

---

## 3. 우선순위 요약

| 순위 | 질문 | 근거 |
|---|---|---|
| 1 | 6/21 이후 **매치가 진짜 0인가, 기록만 0인가?** | 모든 해석이 여기 갈림 (S3 vs S1) |
| 2 | 등록 73% 누수는 **어느 단계**인가? | 가장 큰 절대 손실 (97명) |
| 3 | 6/12 이탈자 9명은 **왜** 안 돌아왔나? | 복귀율 40%의 원인 (S4/S5) |
| 4 | SND 0건은 업로드 문제인가 버그인가? | 파이프라인 신뢰도 (S3) |

---

## 4. 진단 체크리스트 (1~2주 실행 플랜)

### A. 지금 바로 (서버/NeatQueue 접근 필요, ~1시간)
- [ ] `_neatqueue_api_test.py` 또는 `GET /api/v1/history/{server}`로 **6/21 이후 실제 매치 수** 확인 → 기록 커버리지 산출
- [ ] 서버 `decay_state.json`의 `dead_days` / `below_threshold` 확인 — dead-day 면제 정상 작동 여부
- [ ] `/decaystatus`로 6/12 이탈자 9명 샘플 MMR 확인 (800 미만 = 복귀 차단 상태)
- [ ] #results 채널에서 SND 스크린샷 업로드 존재 여부 육안 확인
- [ ] RSVP 명단 크기 vs 실제 참여 인원 (직전 세션들)

### B. 이번 주 (정성 조사)
- [ ] **이탈자 인터뷰**: 6/12 이후 안 돌아온 9명 중 3~5명에게 DM. 질문 3개면 충분 — ① 세션 시간이 맞았나 ② 매치 경험(실력차/대기)이 어땠나 ③ 다시 올 조건은
- [ ] **미참여 등록자 조사**: 등록만 하고 안 뛴 97명 중 5~10명 DM — "등록하고 큐엔 왜 못 들어왔나" (시간대? 800 게이트? 방법을 모름?)
- [ ] 헤비유저 2~3명에게도: 불편한 점, 세션이 안 열린 이유 인식

### C. 계측 추가 (§5) 후 2주 관찰
- [ ] 주간 대시보드 지표 확인 (아래 정의) — 최소 2세션 데이터 쌓일 때까지

### 추적 지표 정의 (주간)
| 지표 | 정의 | 소스 |
|---|---|---|
| 세션 성사율 | 예정 세션 중 매치 1개 이상 성사 비율 | NeatQueue history |
| 세션당 매치/인원 | 성사 매치 수, 고유 참가자 | NeatQueue history |
| 기록 커버리지 | Airtable Match ID ÷ NeatQueue 매치 | 둘 비교 |
| W+1 복귀율 | 이번 세션 참가자 중 다음 세션 재참여 % | Airtable/NQ |
| 신규 전환율 | 신규 Registered 중 14일 내 첫 매치 % | Players + 매치 |
| RSVP 전환율 | RSVP ÷ 실참여 | queue_state + NQ |
| 게이트 차단자 수 | below_threshold 집합 크기 | decay_state |

---

## 5. 계측 공백 메우기 (봇에 추가할 것 — 각각 반나절 이하)

1. **세션 결과 로그**: 매 세션 종료(+3h lock) 시 NeatQueue history를 읽어 `{날짜, 매치 수, 고유 참가자, RSVP 수}`를 Airtable 새 테이블(`Sessions`) 또는 staff 로그에 1행 기록. → 세션 성사율·추이가 자동으로 쌓임
2. **주간 참여 리포트**: 기존 월요일 주간 리더보드 루프에 위 지표 요약 추가 (신규/복귀/이탈 수)
3. **기록 커버리지 알림**: 세션 후 NeatQueue 매치 수 > Airtable Match ID 수면 staff 채널에 "스크린샷 누락 N건" 알림 → S3 완화
4. **(선택) 게이트 차단 알림**: Registered 박탈 시 본인에게 복구 조건 DM — 이미 있는지 확인, 없으면 조용한 박탈은 이탈 확정 요인

---

## 6. 해석 가이드 (체크 결과별 분기)

- **NeatQueue에도 6/21 이후 매치 0** → 문제는 기록이 아니라 **세션 자체 붕괴** (S1). 세션 성사 최소 인원 확보(RSVP 8명 미만이면 사전 취소/연기 공지, 시간대 투표)가 최우선.
- **매치는 있는데 기록만 0** → S3 확정. 스크린샷 의존 줄이기(NeatQueue 데이터 우선, OCR은 보조) 검토.
- **이탈자 다수가 800 미만** → S4 확정. decay 파라미터 완화 또는 시즌 중반 소프트 리셋.
- **인터뷰에서 "시간이 안 맞음" 다수** → S7. 세션 창 추가/이동 투표.
- **"실력차" 다수** → S5. 티어 분리 세션 또는 밸런스 규칙.
